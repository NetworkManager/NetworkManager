// Named configuration, generated by NetworkManager

options {
	directory "/"; 
	query-source address * port 53;
	forward only;
	forwarders { @@FORWARDERS@@ };
	listen-on  { 127.0.0.1; };
	pid-file "@@PID_FILE@@";
};

// Disable rndc
controls { };

@@DOMAIN_ZONES@@
